services:
  # Backend Service - Java Spring Boot API
  - type: web
    name: vicky-salon-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8080
